type User {
	id: ID
	name: String
	avatar: String
	email: String
}

type UsersPage {
  users: [User!]!
  hasMore: Boolean!
	startId: ID
}

type Query {
  usersPage(startId: ID, pageSize: Int!): UsersPage
}
