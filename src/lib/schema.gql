type User {
	id: ID
	name: String
	avatar: String
	email: String
}

type UserPage {
  users: [User!]!
  hasMore: Boolean!
}

type Query {
	#users: [User]
  users(startId: ID, pageSize: Int!): UserPage

}
